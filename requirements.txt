# ShopMate AI - Python Dependencies

# Web framework
fastapi>=0.111.0
uvicorn[standard]>=0.30.1

# HTTP client (async support for Firecrawl)
httpx>=0.27.0

# Google Gemini SDK
google-generativeai>=0.7.2

# Environment variable loading
python-dotenv>=1.0.1

# Data validation (comes with FastAPI but pinned for stability)
pydantic>=2.7.4
